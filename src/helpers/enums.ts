export enum USER_ROLE {
  ADMIN = 'admin',
  SUPER_ADMIN = 'super-admin',
  SELLER = 'seller',
  CUSTOMER = 'customer'
}

export enum USER_STATUS {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  PENDING = 'pending',
  BLOCK = 'block'
}
